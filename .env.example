# Midnight network configuration
# Options: undeployed | testnet | mainnet
MIDNIGHT_NETWORK=testnet

# Midnight testnet public endpoints (replace with mainnet URLs for production)
MIDNIGHT_NODE_URL=https://rpc.testnet.midnight.network
MIDNIGHT_INDEXER_URL=https://indexer.testnet.midnight.network/api/v1/graphql
MIDNIGHT_PROOF_SERVER_URL=https://proof.testnet.midnight.network

# Populated after: npm run deploy
CONTRACT_ADDRESS=
